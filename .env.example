# Supabase — copie ce fichier vers .env et renseigne les valeurs
# (Project Settings → API dans le dashboard Supabase).
# Le préfixe EXPO_PUBLIC_ expose la variable au bundle client.
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
